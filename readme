This mini project is meant to visualize a simple 8x8 chess board with the feauture of changing the board's color.
Using it is obvious - by clicking on the colored buttons you can switch between the three available colors.
